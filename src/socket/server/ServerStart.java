package socket.server;

public class ServerStart {
    /**
     * Main Methode, startet den Server(Backend)
     * @param args (default)
     */
    public static void main(String[] args) {
        ServerBackEnd.start(1312);
    }
}
